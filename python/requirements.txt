# M0/M1 only - by M3 the Python side is stdlib-only and this file is unused.
aiosendspin==6.1.0
# hue-entertainment: install from the local checkout (has ColorMode; pulls
# aiohttp+cryptography+zeroconf):
#   pip install -e ../../hue-entertainment
